<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:background="@color/black">

    <include layout="@layout/vertical_left_guideline"
        android:id="@+id/activity_main__left_guideline"/>
    <include layout="@layout/vertical_right_guideline"
        android:id="@+id/activity_main__right_guideline"/>

    <include layout="@layout/toolbar"
        android:id="@+id/activity_main__toolbar"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/activity_main__categories_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/activity_main__categories"
        android:textStyle="bold"
        android:textColor="@color/white"
        android:textSize="15pt"
        app:layout_constraintLeft_toLeftOf="@id/activity_main__left_guideline"
        app:layout_constraintTop_toBottomOf="@id/activity_main__toolbar"
        android:paddingBottom="3dp"/>

    <ImageView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/activity_main__categories_text_view"
        app:layout_constraintLeft_toLeftOf="@id/activity_main__left_guideline"
        app:layout_constraintRight_toRightOf="parent"
        android:src="@drawable/separator"/>

    <ProgressBar
        android:id="@+id/activity_main__progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:indeterminate="true"/>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/activity_main__categories_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/activity_main__categories_text_view"
        app:layout_constraintBottom_toBottomOf="parent"
        >
    </androidx.recyclerview.widget.RecyclerView>
</androidx.constraintlayout.widget.ConstraintLayout>